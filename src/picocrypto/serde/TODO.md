[ ] Use C API directly (PyList_GET_ITEM, PyDict_Next)
[ ] Avoid repeated isinstance checks
[ ] Batch resize operations
[ ] Consider SIMD for large byte copies
[ ] Pre-allocate buffers when size is predictable
[ ] Pool and reuse buffers
[ ] Use branch prediction hints for common cases
[ ] Specialize for homogeneous data